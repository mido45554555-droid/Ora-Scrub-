// Plain stylesheet imports (e.g. `import '../globals.css'` in the root
// layout). Next.js bundles them; this only tells TypeScript 6+, which
// checks side-effect imports by default, that they're valid modules.
declare module '*.css';
