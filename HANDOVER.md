# تسليم مشروع ORA

دليل تشغيل المشروع من الصفر على جهاز جديد. الشرح بالعربي والأوامر بالإنجليزي.

## المشروع بيتكوّن من إيه

```
frontend/   الموقع: Next.js 16 · React 19 · Tailwind · next-intl (عربي/إنجليزي)
backend/    الـ API: Node.js · Express 5 · MariaDB/MySQL
```

العميل بيملا فورم الطلب على الموقع، الموقع بيبعت الطلب لـ `/api/order`
عنده، ودي بتحوّله للباك إند على `127.0.0.1:4000`. الباك إند بيتأكد من
البيانات، ويحفظ الطلب في الداتابيز والصور في `backend/storage/`، وبعدها
يبعت إيميل بالطلب لصاحب المحل.

**الباك إند مش مفتوح على النت:** بيسمع على `127.0.0.1` بس، وأي طلب
لازم يجيب معاه مفتاح سري الموقع بس اللي عارفه.

## المتطلبات

- Node.js 20.12 أو أحدث (المشروع متجرب على 24)
- MariaDB أو MySQL شغال (XAMPP مظبوط للشغل ده)

## التشغيل

### 1) الباك إند

```bash
cd backend
npm install
npm run setup                    # بيعمل .env وقاعدة البيانات ويوزر محدود الصلاحيات والجداول
npm run create-admin -- owner    # حساب أدمن، هيطلب باسورد 12 حرف على الأقل
npm start
```

`npm run setup` بيعمل لوحده:

- ملف `.env` من `.env.example` وجواه `INTERNAL_API_KEY` و`DB_PASSWORD`
  **عشوائيين جداد** (مش محتاج حد يبعتهملك)
- قاعدة بيانات `ora_scrubs` ويوزر `ora_app` صلاحياته SELECT/INSERT/UPDATE/DELETE
  على القاعدة دي بس، مش root
- جداول المشروع

لو MySQL عندك بباسورد لـ root، غيّر `DB_ROOT_USER` و`DB_ROOT_PASSWORD` في
`.env` قبل ما تشغّل `npm run setup`.

### 2) الموقع

```bash
cd frontend
npm install
cp .env.example .env.local
```

افتح `.env.local` وحط فيه:

```
ORDER_BACKEND_URL=http://127.0.0.1:4000
ORDER_BACKEND_API_KEY=<نفس INTERNAL_API_KEY اللي في backend/.env>
```

**القيمتين لازم يكونوا متطابقين**، وإلا الطلبات هترجع 401. بعدها:

```bash
npm run dev      # أو: npm run build && npm start
```

الموقع على `http://localhost:3000`، وبيحوّلك لـ `/ar` أو `/en` حسب لغة المتصفح.

### 3) إيميل الطلبات (اختياري بس مهم)

من غيره الطلبات بتتحفظ عادي، بس محدش بيتبلّغ بيها. في `backend/.env`:

```
ORDER_NOTIFY_TO=orascrubs@gmail.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=orascrubs@gmail.com
SMTP_PASS=<App Password من جوجل، 16 حرف>
```

الـ App Password بيتعمل من <https://myaccount.google.com/apppasswords> بعد
تفعيل التحقق بخطوتين على الحساب، وهو **مش** باسورد الجيميل العادي.
للتأكد إنه شغال:

```bash
cd backend
npm run mail:test     # بيسجل دخول ويبعت رسالة اختبار
```

بديل مجاني تاني: Brevo (300 إيميل/يوم) — الإعدادات في تعليقات `.env.example`.

## حدود الطلبات وكوكي الجهاز

كل طلب بيعدّي على 3 حدود، وكلها في `backend/.env`:

| الإعداد | الافتراضي | المعنى |
|---|---|---|
| `ORDER_RATE_LIMIT` | 5 | طلبات لكل **متصفح** كل ربع ساعة |
| `ORDER_IP_RATE_LIMIT` | 30 | طلبات لكل **IP** كل ربع ساعة |
| `ORDER_GLOBAL_RATE_LIMIT` | 100 | إجمالي الطلبات كل ربع ساعة |
| `UPLOAD_CONCURRENCY` | 64 | رفعات في نفس الوقت |

الحد الأساسي على المتصفح مش على الـ IP، لأن شبكات المحمول في مصر
بتشارك نفس الـ IP بين آلاف المستخدمين، فحد على الـ IP لوحده كان
هيمنع عملاء حقيقيين. الموقع بيحفظ كوكي `ora_device` موقّعة من السيرفر
(httpOnly ومفيهاش أي بيانات شخصية)، والسيرفر بيرفض أي كوكي متزوّرة
ويدي المتصفح واحدة جديدة.

الصور بتترفع على القرص مباشرة مش في الذاكرة، فالسيرفر بيستحمل رفعات
كتير في نفس الوقت من غير ما الذاكرة تكبر.

## الأوامر

| الأمر | المكان | بيعمل إيه |
|---|---|---|
| `npm start` | backend | تشغيل الـ API |
| `npm run dev` | backend | تشغيل مع إعادة تحميل عند التعديل |
| `npm test` | backend | 26 اختبار (محتاج: `DB_NAME=ora_scrubs_test npm run setup` مرة واحدة) |
| `npm run mail:test` | backend | اختبار إعدادات الإيميل |
| `npm run create-admin -- <اسم>` | backend | إنشاء أدمن أو تغيير باسورده |
| `npm run dev` | frontend | تشغيل الموقع |
| `npm run build` | frontend | بناء نسخة الإنتاج |
| `npm run lint` | frontend | فحص الكود |
| `npm run typecheck` | frontend | فحص الأنواع |

## بيانات لازم تيجي من صاحب المشروع

مش موجودة في الكود، ومحطوط مكانها قيم مؤقتة:

- **رقم فودافون كاش وحساب إنستاباي الحقيقيين** — في `frontend/lib/constants.ts`
  (`VODAFONE_CASH_NUMBER` و`INSTAPAY_ACCOUNT`). غيّر القيمتين دول بس.
- **باسورد الجيميل (App Password)** للإيميل.
- بيانات التواصل، وصور المنتجات، وقائمة الخامات والموديلات، والأسعار.

## حاجات لسه متعملتش

- **واجهة لوحة الأدمن**: الـ API كامل وموجود (تسجيل دخول، عرض الطلبات
  والبحث، تغيير الحالة، عرض الصور) — موثّق في `backend/README.md` — بس
  مفيش صفحة واجهة ليه لسه. دلوقتي الطلبات بتتشاف من الإيميل أو phpMyAdmin.
- صور منتجات حقيقية: الصفحة الرئيسية فيها رسومات مؤقتة
  (`frontend/components/ui/PlaceholderArt.tsx`).
- صفحة "تواصل معنا" مؤقتة لحد ما بيانات التواصل تتحدد.

## نقط مهمة في الكود

- **قواعد التحقق متكررة في مكانين عن قصد**: `frontend/lib/validation/order.ts`
  (تجربة أحسن للمستخدم) و`backend/src/validation/order.js` (دي الحماية
  الحقيقية). لو غيّرت قاعدة، غيّرها في الاتنين.
- **رسايل الأخطاء أكواد مش جُمل** (زي `min:50`)، والموقع بيترجمها من
  `frontend/messages/ar.json` و`en.json`. أي نص جديد لازم يتحط في
  الملفين، والمفاتيح لازم تفضل متطابقة.
- **الصور**: النوع بيتحدد من محتوى الملف نفسه مش من امتداده، وبتتخزن
  بأسماء عشوائية في فولدر مش مفتوح على الويب، وبتتعرض بس من API الأدمن
  بعد تسجيل الدخول.
- **الأسرار**: كل حاجة في `.env` و`.env.local`، والملفات دي مستثناة من git
  وماينفعش ترفعها.
- الأمان بالتفصيل، وقائمة اللي لازم يتعمل قبل النشر على النت، في
  `backend/README.md`.
